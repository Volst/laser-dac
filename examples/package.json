{
  "name": "@laser-dac/examples",
  "version": "0.0.7",
  "private": true,
  "license": "MIT",
  "repository": "Volst/laser-dac",
  "engines": {
    "node": ">=8.0"
  },
  "dependencies": {
    "@types/victor": "^1.1.0",
    "ts-node-dev": "^1.0.0-pre.30",
    "victor": "^1.1.0"
  },
  "scripts": {
    "start": "ts-node-dev --no-notify --project ../tsconfig.json",
    "watch": "if [[ ${EXAMPLE} ]]; then yarn start $EXAMPLE; else echo; fi"
  }
}
