include_directories(..)

set(LASERDOCKLIB_TEST_SOURCE_FILES main.cpp)
if (ANDROID)
    add_library(laserdocklibtest SHARED ${LASERDOCKLIB_TEST_SOURCE_FILES})
else()
    add_executable(laserdocklibtest ${LASERDOCKLIB_TEST_SOURCE_FILES})
endif()
target_link_libraries(laserdocklibtest laserdocklib)

set(LASERDOCKLIB_CIRCLE_SOURCE_FILES laserdocklibcircle.cpp)
if (ANDROID)
    add_library(laserdocklibcircle SHARED ${LASERDOCKLIB_CIRCLE_SOURCE_FILES})
else()
    add_executable(laserdocklibcircle ${LASERDOCKLIB_CIRCLE_SOURCE_FILES})
endif()
target_link_libraries(laserdocklibcircle laserdocklib)

set(LASERDOCKLIB_DEVICEMANAGERTEST_SOURCE_FILES laserdockdevicemanagertest.cpp)
if (ANDROID)
    add_library(LaserdockDeviceManagerTest SHARED ${LASERDOCKLIB_DEVICEMANAGERTEST_SOURCE_FILES})
else()
    add_executable(LaserdockDeviceManagerTest ${LASERDOCKLIB_DEVICEMANAGERTEST_SOURCE_FILES})
endif()
target_link_libraries(LaserdockDeviceManagerTest laserdocklib)

set(LASERDOCKLIB_RUNNER_SOURCE_FILES runnertest.cpp)
if (ANDROID)
    add_library(LaserdockRunnerTest SHARED ${LASERDOCKLIB_RUNNER_SOURCE_FILES})
else()
    add_executable(LaserdockRunnerTest ${LASERDOCKLIB_RUNNER_SOURCE_FILES})
endif()
target_link_libraries(LaserdockRunnerTest laserdocklib)
